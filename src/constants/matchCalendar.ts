/** Device calendar title (syncs to Google Calendar when the account is on the device). */
export const SHOT_VISION_CALENDAR_TITLE = "Shot Vision";

/** Default block length for a scheduled tennis match. */
export const MATCH_CALENDAR_DURATION_MINUTES = 90;

/** Deep link base for event notes (Expo Router). */
export const MATCH_CALENDAR_DEEP_LINK_BASE = "shotvision://match";
